doctype html
head
  title Login page

  link(rel="stylesheet" href="css/login.css")

body
  form#login(
    action="/login"
    method="POST"
  )
    label(for="login-name") Login name:
    input#username(type="text" required name="username")
    label(for="password") Password:
    input#password(type="password" required name="password")
    button Login